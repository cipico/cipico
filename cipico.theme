<?php

/**
 * @file
 * Theme functions.
 */
function cipico_element_info_alter(array &$types) {
    $test = "";
    if (isset($types['table'])) {
      $types['table']['#attached']['library'][] = 'your_module/library_name';
    }
  }